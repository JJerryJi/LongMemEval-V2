# Learned Retrieval Strategy

This file contains online retrieval hints learned from previous memory queries in this run. Treat it as retrieval guidance, not as final answers.

## Past Queries

| Looking for | Evidence found | Fast path |
|---|---|---|
| New fields added to the `Order Summary` after clicking `Next` on the Magento checkout page | trajectory `1e58cea9`, states `13-14`, the Shipping step advances to Review & Payments and the `Order Summary` table adds `Cart Subtotal`, `Shipping Flat Rate - Fixed`, and `Order Total` | inspect the first `Next` transition span first; if the page moves from Shipping to Review & Payments, reuse the `Order Summary` rows directly instead of broad UI searching |
| The print-related link directly below `Print receipt` on the Magento order confirmation page | trajectory `1e58cea9`, state `15`, the confirmation page shows `Print receipt` and `Continue Shopping`, with the order-number link in between, so the visible link beneath the print section is `Continue Shopping` | inspect the order-success confirmation state first; if the page layout matches this span, read the visible link below the print section directly instead of assuming adjacency |
| The sum of upvotes and downvotes on a newly submitted Reddit-style forum post | trajectory `4ba5e9cb`, state `4`, the submitted post page for `places for new drivers to learn driving in pittsburgh` shows `1 point (+1, −0)` in the vote summary | inspect the post-display page right after submission; if the vote summary is visible, read the `point` total directly instead of counting vote widgets separately |
| A previous order within a timeframe that contains a specific item on the custom shopping site | trajectory `6d5f3f24`, states `10-11`, `My Orders` was only a table with `Order #`, `Date`, `Order Total`, `Status`, and `View Order` / `Reorder`, and the opened order detail page showed the `Items Ordered` table | inspect `My Orders` first; if it is only a dated order table, scan by date and open candidate orders one by one to verify the item in the detail page |
| ServiceNow `Expense Line` form `Summary type` business options | trajectory `e72dc073`, state `70`, the open `Summary type` combobox on the record page showed `-- None --`, `Grow Business`, `Run Business`, `Transform Business` in order | inspect the open field control on the record form first; if the combobox is visible, read the dropdown entries directly and preserve their order, excluding `-- None --` when the query asks for business options only |
| ServiceNow expense-line question about removing duplicated existing expenses and the fields most important in the typical workflow | trajectory `e72dc073`, state `3`, the `Managing Your Existing Expenses` knowledge article says duplicates are identified by the same short description and resolved using task linkage, oldest date, and highest amount tie-breaks | inspect the knowledge article state first; reuse the article rules to infer the core fields (`Short description`, `Task`, `Date`, `Amount`) instead of hunting the form for a direct field list |
| Magento category-page price filtering with editable min/max boxes | trajectory `dddd8aa2`, states `4-5`, the category page showed `Shopping Options -> Price` as clickable price buckets like `$0.00 - $99.99` and `$100.00 - $199.99`, and a later state used a `price=0-30` URL filter instead of text boxes | inspect this span first; if the current query assumes min/max price inputs, reuse the span as a mismatch check and abstain rather than searching broadly |
| Magento category-page price bucket selection that reveals follow-on links | trajectory `dddd8aa2`, state `5`, after selecting `Price: $0.00 - $29.99` the `Now Shopping by` block shows `Remove This Item` and `Clear All` immediately after the selected range | inspect the post-filter state first; if the query asks for links that appear after a chosen price range, reuse the `Now Shopping by` span directly instead of re-running the category search |
| ServiceNow loaner-laptop form with a preset duration and typed date field | trajectory `013696c4`, state `42`, the `Loaner Laptop` catalog item shows `How long do you need it for ?` as a combobox with preset values like `1 day`, `1 week`, `2 weeks`, `3 days`, and `1 month`, while `When do you need it ?` is a textbox for a specific date | inspect the laptop catalog form directly; if the question is about duration-vs-date input types, reuse this state and answer from the field widgets instead of assuming both are date pickers or both are preset lists |
| ServiceNow user deletion auto-affected records, especially VPN-token | trajectory `f92c1020`, states `46-47`, the delete confirmation dialog explicitly listed the auto-deleted record types as `Notification Device`, `Expense Line`, and `User Preference` and did not mention VPN-token | inspect the deletion confirmation span first; if the exact record type is missing from the explicit list, treat that as evidence against the premise rather than searching wider |
| The title of the small popup that appears under ServiceNow `Assigned to` before typing in an incident form | trajectory `9c420e86`, states `28-31`, the `Assigned to` field opened as an autocomplete combobox and expanded into the `AC incident assigned_to` listbox; the visible popup title to reuse was `Assigned to` | inspect the incident-form span first; if the field is an autocomplete combobox with an empty pre-input popup, reuse the field label as the box title instead of broad searching |
| Whether a ServiceNow Macbook-style developer laptop order exposes SSD size as a configurable choice | trajectory `3c588c61`, states `1-15`, and trajectory `096432bf`, states `1-17`, the Service Catalog / Requested Items flow reaches the laptop order page and the configuration section includes `What size solid state drive do you want?` with values like `250` or `500` | inspect the item page after the catalog/request lookup; if the query is about the laptop order form, reuse the SSD field label directly instead of broad searching or switching to hardware asset records |
| The next field to prioritize for duplicate-expense cleanup on the ServiceNow `Expense Lines` page after `Amount`, `Task`, `Short description`, and `Date` | trajectory `0c80a9e1`, state `8`, the searchable field selector lists `Number`, `User`, `Inherited`, `Parent`, `Date`, `Short description`, `Source ID`, `Amount`, `Type`, and `Summary type` | inspect the field selector state first; reuse `Parent` as the `Task` alias and take `Source ID` as the next field beyond the common duplicate-check fields |
| A reddit-style custom forum biography-edit question asks for the button name in the upper-right profile block after saving | trajectory `3331c568`, state `5`, the right-hand profile/complementary block updates after `Save` and shows the clickable label `Edit biography` | inspect the post-save right-hand block first; if it updates with a single link/button label, reuse that label directly instead of reading the preview area below the text box |
| Comparing ServiceNow `Create Incident` vs `Problem` forms for a bottom-page `Related Search` feature | trajectories `1d1cac57` and `13083bae`, state `0` on both, each initial form view already shows `Related Search` and `Related Search Results` at the bottom | inspect the initial load state for each form first; if the panel is already visible, count it directly instead of searching for hidden tabs or expansion controls |
| The action that appears between `Reorder` and `Ship` on Magento admin order detail pages | trajectory `11c4dfb8`, state `5`, the order detail toolbar shows `Invoice`, `Ship`, and `Reorder` together, so the middle action is `Invoice` | inspect the order detail toolbar on the opened order first; if the page is the order view rather than the orders grid, read the visible top actions directly instead of inferring from list pages |
| The name of a Magento admin dashboard tab by relative position in the visible row | trajectory `366f17da`, state `0`, the dashboard tab strip shows `Bestsellers`, `Most Viewed Products`, `New Customers`, and `Customers`, so the second from the right is `New Customers` | inspect the initial dashboard state first; if the tab row is fully visible, count from the right edge instead of searching other admin pages |
| The link shown in the blue notice bar on a single comment thread page in the reddit-based custom forum | trajectory `2e8f6477`, state `9`, the page shows `Viewing a single comment thread.` and the clickable link `View all comments` immediately below it | inspect the single-thread state first; if the notice bar is present, reuse the adjacent `View all comments` link text directly instead of broad searching the thread tree |

## Strategies

| When | Try first | Gotcha |
|---|---|---|
| A Magento checkout question asks what appears in `Order Summary` after pressing `Next` from Shipping | inspect the state transition span that crosses the `Next` click and read the `Order Summary` table on the review page | do not guess from the shipping step alone; the answer is often only visible after the page advances to Review & Payments |
| A Magento order-confirmation question asks for a link positioned relative to `Print receipt` | inspect the success-page span and verify intervening links before answering | do not assume the next visible link is directly adjacent; the order-number link can sit between `Print receipt` and `Continue Shopping` |
| A Reddit-style forum question asks for the sum of upvotes and downvotes on a new post | inspect the post page immediately after submission and read the vote summary line, especially the `point` total | do not count UI affordances separately if the page already exposes a `1 point (+1, −0)`-style summary; use the displayed total |
| A query asks for dropdown/combobox options on a ServiceNow record form | open the selected trajectory span and read the visible control state directly, including option order and any `-- None --` sentinel | do not infer from the field label or from another record type; record forms often expose the answer only after the control is opened |
| A query names a specific UI control but the haystack may implement a different control family | check `summary.json` and the selected trajectory span before broad search; look for the actual control labels or URL parameters | do not transfer a plausible interaction from the question text to the page if the span shows bucket links, dropdowns, or URL-based filtering instead |
| A Magento product-list query mentions price filters | inspect the category-page span for `Shopping Options -> Price` and any `price=...` URL state before hunting for input fields | editable min/max boxes are a near-match trap here; the correct evidence may be a bucket list or a direct URL filter, so the safest output can be abstain |
| A Magento price-bucket query asks which links appear after selecting a range | inspect the post-filter `Now Shopping by` block in the same state as the selected price range | the answer is often the two maintenance links next to the selected range, not another category or product-list link |
| A ServiceNow deletion question asks which related records are automatically affected | inspect the user-delete confirmation dialog span before broad search | the dialog often enumerates the full set directly; if the named record type is not in the list, do not infer it from the generic "related records" wording |
| A WebArena shopping query asks for an order containing an item within a date range | check whether `My Orders` only provides date, status, and `View Order`; if so, the item search happens only after opening candidate orders | do not assume there is a direct timeframe/item filter on the orders list; the correct path may be date scan plus per-order item inspection |
| A ServiceNow incident form shows an `Assigned to` autocomplete popup before any typing | inspect the field state and popup title together; the popup title is usually the field label, while the suggestion list may carry a separate `AC ...` listbox name | do not overfit the listbox role name to the visible title; the user-facing box title can stay `Assigned to` even when the internal listbox has a different label |
| A ServiceNow laptop-order query asks whether SSD size is configurable | search the `Service Catalog` / `Requested Items` flow for the exact laptop product name first, then inspect the order form for `What size solid state drive do you want?` | do not map the prompt to a different laptop label or to a hardware asset page; the evidence lives in the order form configuration, not the generic asset record |
| A ServiceNow loaner-laptop query asks whether duration is preset and date is typed | inspect the `Loaner Laptop` catalog item form and read the control types directly: duration is a preset combobox, date is a free-text textbox | do not flatten both prompts into the same widget type; the exact answer depends on the field widget, not just the presence of a date-related label |
| A ServiceNow expense duplicate-removal query asks which fields matter most in the workflow | inspect the `Managing Your Existing Expenses` article before the incident/form page; the decision rule itself names the fields that matter | do not overfit to the page title or to a generic expense form; the reusable signal is the duplicate-resolution rule set, not the UI control labels |
| A ServiceNow `Expense Lines` duplicate-cleanup query asks for the next field after the common duplicate-check fields | inspect the field selector/open filter list on the expense line page and map `Task` to the selector’s `Parent` label before deciding | do not skip the alias check; the field may be shown under a different name than the question uses, and `Source ID` can be the next actionable field |
| A reddit-style custom forum biography-edit query asks for the button name after saving | inspect the post-save right-hand profile block first; the actionable label there is the answer, while the preview below the editor is a distractor | do not infer from the biography text area or preview block, since the visible control is often only in the adjacent profile card after save |
| A ServiceNow form-comparison query asks whether `Create Incident` or `Problem` has `Related Search` at the bottom | inspect state `0` for both trajectories and check the visible footer panels directly | do not assume the feature is hidden or requires interaction; both forms can expose `Related Search` immediately on load |
| A Magento admin order-detail query asks for the action between two named toolbar buttons | inspect the opened order detail page and read the top toolbar action order directly | do not transfer the action order from the Sales > Orders grid or from another status; the toolbar is visible on the order view itself and may be enough to answer without any extra navigation |
| A Magento admin dashboard query asks for a tab by relative position in the visible tab row | inspect the dashboard's initial state and count tabs from the right edge | do not navigate away when the row is already visible; the answer is usually a direct position read, not a hidden menu or secondary page |
| A reddit-based forum query asks for the link associated with the blue single-thread notice above `View all comments` | inspect the single comment thread state and read the link text adjacent to the notice | do not confuse the notice text `Viewing a single comment thread.` with the answer; the reusable target is the link label itself |
