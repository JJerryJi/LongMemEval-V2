# Learned Retrieval Strategy

This file contains online retrieval hints learned from previous memory queries in this run. Treat it as retrieval guidance, not as final answers.

## Past Queries

| Looking for | Evidence found | Fast path |
|---|---|---|
| Which two controls open the nested reply composer for an existing comment and then publish the reply on the reddit-based custom forum | trajectory `2e8f6477`, states `7-8`, on the AskReddit comment thread the inline comment-level `Reply` control opens the nested composer and the composer exposes `Post` to publish | inspect the existing comment first; if the inline `Reply` link is present, reuse that span and then read the nested composer footer for `Post` instead of searching the whole post composer |
| The inventory/order dashboard report prefix in `Reports > View / Run` | trajectory `013696c4`, state `28`, the `Reports` list shows the report link `Catalog with hashtag #CAT001314192`, so the inventory/order family uses the `Catalog` prefix | inspect the `Reports > View / Run` list row directly; reuse the report title prefix from the visible link instead of opening the chart, and do not carry the incident-assignment `Incidents` prefix over to inventory/order reports |
| The forum with the second-most submissions on the reddit-based custom forum homepage | trajectory `56b3344d`, state `0`, the Postmill homepage/subscribed feed shows repeated submissions from `space` and several from `books`; `space` is most frequent in the visible feed and `books` is second | inspect the initial homepage/feed state first; if the visible submission titles repeat forum names, count the repeats directly instead of searching deeper pages |
| Whether a Magento Contact Us page contains a customer-service phone number below the form | trajectory `6d5f3f24`, state `21`, the Contact Us page shows a `Write Us` form with `Name`, `Email`, `Phone Number`, and `What’s on your mind?` fields plus `Submit`, and no support phone number is displayed | inspect the Contact Us page directly; if the visible controls are just the form fields and footer links, treat scrolling as unlikely to reveal a customer-service number |
| The field name on the Magento bulk-attribute update details page that shows whether the update succeeded after clicking `View Details` | trajectory `e9a95d4f`, states `23-25`, the post-save `View Details` dialog shows the success text in a field labeled `Summary` with value `1 items selected for mass update, 1 successfully updated` | inspect the `View Details` dialog first; if the success text is already visible in the dialog, reuse the `Summary` label directly instead of searching for a `Result` field |
| New fields added to the `Order Summary` after clicking `Next` on the Magento checkout page | trajectory `1e58cea9`, states `13-14`, the Shipping step advances to Review & Payments and the `Order Summary` table adds `Cart Subtotal`, `Shipping Flat Rate - Fixed`, and `Order Total` | inspect the first `Next` transition span first; if the page moves from Shipping to Review & Payments, reuse the `Order Summary` rows directly instead of broad UI searching |
| the minimum action count to sum the latest twelve orders on the Magento storefront | trajectory `06a17a60`, states `0-2`, the home page exposes `My Account`, the account page exposes `View All` under `Recent Orders`, and the `My Orders` page shows `Items 1 to 10 of 37 total` with a `Page 2` link | inspect `My Account` first, then `View All` / `My Orders`; if the list only shows 10 orders per page, continue to page 2 before counting the latest twelve orders |
| The print-related link directly below `Print receipt` on the Magento order confirmation page | trajectory `1e58cea9`, state `15`, the confirmation page shows `Print receipt` and `Continue Shopping`, with the order-number link in between, so the visible link beneath the print section is `Continue Shopping` | inspect the order-success confirmation state first; if the page layout matches this span, read the visible link below the print section directly instead of assuming adjacency |
| Which field names are absent from an individual review block on a Magento product detail page | trajectory `8a6653c8`, state `1`, the `Reviews (3)` tab is open and each review card shows review title, star rating, review text, `Review by` name, and `Posted on` date; the block does not show a verified purchase badge, detailed rating scores, or a product recommendation indicator | inspect the open `Reviews` tab and read one review card end-to-end; reuse the visible card labels directly and do not promote page-level review text into extra per-review fields |
| The sum of upvotes and downvotes on a newly submitted Reddit-style forum post | trajectory `4ba5e9cb`, state `4`, the submitted post page for `places for new drivers to learn driving in pittsburgh` shows `1 point (+1, −0)` in the vote summary | inspect the post-display page right after submission; if the vote summary is visible, read the `point` total directly instead of counting vote widgets separately |
| The separate downvote count shown next to a newly submitted Reddit-style forum post | trajectory `4ba5e9cb`, state `4`, the submitted post page for `places for new drivers to learn driving in pittsburgh` shows `1 point (+1, −0)`, so the explicit downvote count is `0` | inspect the post-display page right after submission; if the `(+1, −0)` summary is visible, reuse the separate `−0` count directly instead of inferring from vote buttons |
| A previous order within a timeframe that contains a specific item on the custom shopping site | trajectory `6d5f3f24`, states `10-11`, `My Orders` was only a table with `Order #`, `Date`, `Order Total`, `Status`, and `View Order` / `Reorder`, and the opened order detail page showed the `Items Ordered` table | inspect `My Orders` first; if it is only a dated order table, scan by date and open candidate orders one by one to verify the item in the detail page |
| ServiceNow `Expense Line` form `Summary type` business options | trajectory `e72dc073`, state `70`, the open `Summary type` combobox on the record page showed `-- None --`, `Grow Business`, `Run Business`, `Transform Business` in order | inspect the open field control on the record form first; if the combobox is visible, read the dropdown entries directly and preserve their order, excluding `-- None --` when the query asks for business options only |
| The `Expense Line` record-page field that has a checkbox next to it | trajectory `1046053f`, state `27`, the record page directly shows `Inherited` rendered as a checkbox under the record header | inspect the base record form first; if `Inherited` is visibly a checkbox under the header, reuse the label directly instead of searching the list view or selector spans |
| The two fields on the `Private Task` form page that have a checkbox next to them | trajectory `454485ca`, state `0`, the Private Task form section directly shows `Active` and `Work notes` as the checkbox fields | inspect the `Private Task` form section at state `0`; if the same page is present, reuse these two labels directly |
| ServiceNow expense-line question about removing duplicated existing expenses and the fields most important in the typical workflow | trajectory `e72dc073`, state `3`, the `Managing Your Existing Expenses` knowledge article says duplicates are identified by the same short description and resolved using task linkage, oldest date, and highest amount tie-breaks | inspect the knowledge article state first; reuse the article rules to infer the core fields (`Short description`, `Task`, `Date`, `Amount`) instead of hunting the form for a direct field list |
| The first page to open for the ServiceNow incident-assignment protocol that routes incidents by category and priority | trajectory `fa42acc1`, state `3`, the `Knowledge & Catalog - Knowledge` search results show `Work Assignment: Assign Incidents to Relevant Agents` (`KB0010138`); the list-item snippet says the article assigns incidents based on category and priority | inspect the knowledge search results first; if the exact article title or `KB0010138` is visible, reuse it directly instead of hunting the incident list or a generic assignment page |
| The class assigned to mice and keyboards on the ServiceNow asset management page | trajectory `49f98e9e`, state `3`, the ServiceNow `Assets` list shows both `Logitech Desktop Optical Wireless Mouse` and `Logitech Logitech Desktop Keyboard`, and each row's `Class` column is `Consumable` | inspect the asset list rows directly; if the mouse and keyboard rows are visible, read the `Class` column in place instead of opening the asset records |
| Magento category-page price filtering with editable min/max boxes | trajectory `dddd8aa2`, states `4-5`, the category page showed `Shopping Options -> Price` as clickable price buckets like `$0.00 - $99.99` and `$100.00 - $199.99`, and a later state used a `price=0-30` URL filter instead of text boxes | inspect this span first; if the current query assumes min/max price inputs, reuse the span as a mismatch check and abstain rather than searching broadly |
| Magento category-page price bucket selection that reveals follow-on links | trajectory `dddd8aa2`, states `3-5`, after clicking the left-column `Price: $0.00 - $29.99` bucket on the Men > Shoes category page, the `Now Shopping by` block shows the selected range followed by the two links `Remove This Item` and `Clear All` | inspect the post-filter state first; if the query asks for links that appear after a chosen price range, reuse the `Now Shopping by` span directly instead of re-running the category search |
| Exact ServiceNow Service Catalog item names for the Mac and PC developer laptop items | trajectory `787ceaeb`, state `14`, the Service Catalog result page shows the link `Developer Laptop (Mac)`; trajectory `3c588c61`, state `34`, the Service Catalog result page shows the link `Development Laptop (PC)` | inspect the catalog search results first; if the question asks for the two similar laptop item names, reuse the exact visible labels and join them with a semicolon |
| The default number of Service Catalog search results shown per page | trajectory `2083b6e5`, state `5`, the Service Catalog search results page shows the rows-per-page selector set to `20 per page`, with other options `10`, `15`, `20`, `50`, and `100` | inspect the selector on the results page first; if it is visible, reuse the selected value directly instead of counting rows or assuming a default from other lists |
| Whether Google Chrome is available as optional software on the Macbook developer laptop order | trajectory `787ceaeb`, state `14`, the `Developer Laptop (Mac)` catalog item page lists `Optional Software` choices `Adobe Acrobat`, `Adobe Photoshop`, and `Eclipse IDE`; `Google Chrome` is not listed | inspect the exact Mac laptop item page and read the `Optional Software` checkboxes directly; if Chrome is absent from that visible list, treat the premise as false instead of borrowing options from another laptop item |
| ServiceNow loaner-laptop form with a preset duration and typed date field | trajectory `013696c4`, state `42`, the `Loaner Laptop` catalog item shows `How long do you need it for ?` as a combobox with preset values like `1 day`, `1 week`, `2 weeks`, `3 days`, and `1 month`, while `When do you need it ?` is a textbox for a specific date | inspect the laptop catalog form directly; if the question is about duration-vs-date input types, reuse this state and answer from the field widgets instead of assuming both are date pickers or both are preset lists |
| ServiceNow user deletion auto-affected records, especially VPN-token | trajectory `f92c1020`, states `46-47`, the delete confirmation dialog explicitly listed the auto-deleted record types as `Notification Device`, `Expense Line`, and `User Preference` and did not mention VPN-token | inspect the deletion confirmation span first; if the exact record type is missing from the explicit list, treat that as evidence against the premise rather than searching wider |
| After creating a new ServiceNow User record and submitting it, which automatic action is confirmed by the top-page popup message | trajectory `132da65a`, state `8`, the post-submit top-page info message says `Primary email device created for Ann-Erin Howard-Bauer`, which directly confirms email device creation rather than a generic insert/update message | inspect the success popup/info banner immediately after submit; if it names a specific auto-created record, map that exact wording to the answer instead of defaulting to `Record inserted` or `Update successful` |
| The title of the small popup that appears under ServiceNow `Assigned to` before typing in an incident form | trajectory `9c420e86`, states `28-31`, the `Assigned to` field opened as an autocomplete combobox and expanded into the `AC incident assigned_to` listbox; the visible popup title to reuse was `Assigned to` | inspect the incident-form span first; if the field is an autocomplete combobox with an empty pre-input popup, reuse the field label as the box title instead of broad searching |
| Whether a ServiceNow Macbook-style developer laptop order exposes SSD size as a configurable choice | trajectory `3c588c61`, states `1-15`, and trajectory `096432bf`, states `1-17`, the Service Catalog / Requested Items flow reaches the laptop order page and the configuration section includes `What size solid state drive do you want?` with values like `250` or `500` | inspect the item page after the catalog/request lookup; if the query is about the laptop order form, reuse the SSD field label directly instead of broad searching or switching to hardware asset records |
| Whether the Dell XPS 13 developer laptop order exposes SSD size as a configurable choice | trajectory `096432bf`, states `24-25`, the `Development Laptop (PC)` / Dell XPS 13 item page shows `What size solid state drive do you want?` with `250 GB` and `500 GB` options, which supports `true` | inspect the exact item page after the catalog lookup; if the question is a true/false configurability check, reuse the field label and visible options directly |
| The extra dollar amount for the largest SSD option on a ServiceNow developer laptop order | trajectory `3c588c61`, state `35`, the `Development Laptop (PC)` / Dell XPS 13 item page shows SSD choices in the configuration section, and the largest option is `500 GB [add $300.00]` | inspect the exact item page after the catalog lookup; if the SSD radio already includes a price delta, reuse that label directly instead of checking a hardware record or recomputing from the base price |
| The `250 GB` SSD price delta after switching a Dell XPS 13 order to `500 GB` | trajectory `096432bf`, state `25`, on the Dell XPS 13 item page after selecting `500 GB`, the `250 GB` SSD option is relabeled `250 GB [subtract $300.00]`; the prompt's operating-system wording points at the wrong control, but the bracketed amount is on the SSD option | inspect the exact SSD option label after the larger drive is selected; if the question mentions OS but asks about the bracket next to `250 GB`, reuse the SSD radio label directly |
| Comparing several ServiceNow hardware catalog item pages to find which one needs the most visible pre-`Order Now` changes | trajectory `4919aae9`, state `81`, plus comparison spans `2083b6e5` state `6`, `013696c4` states `42-44`, `12c22166` states `3-4`, and `787ceaeb` state `14`; `Sales Laptop` had the richest surface with four `Optional Software` checkboxes, an `Additional software requirements` textbox, and `Quantity`, while the other item pages were simpler | inspect the full item page before `Order Now`; if `Sales Laptop` is one of the candidates, check it first and count distinct visible widgets on the item page rather than relying on the catalog title alone |
| The next field to prioritize for duplicate-expense cleanup on the ServiceNow `Expense Lines` page after `Amount`, `Task`, `Short description`, and `Date` | trajectory `0c80a9e1`, state `8`, the searchable field selector lists `Number`, `User`, `Inherited`, `Parent`, `Date`, `Short description`, `Source ID`, `Amount`, `Type`, and `Summary type` | inspect the field selector state first; reuse `Parent` as the `Task` alias and take `Source ID` as the next field beyond the common duplicate-check fields |
| A reddit-style custom forum biography-edit question asks for the button name in the upper-right profile block after saving | trajectory `3331c568`, state `5`, the right-hand profile/complementary block updates after `Save` and shows the clickable label `Edit biography` | inspect the post-save right-hand block first; if it updates with a single link/button label, reuse that label directly instead of reading the preview area below the text box |
| Comparing ServiceNow `Create Incident` vs `Problem` forms for a bottom-page `Related Search` feature | trajectories `1d1cac57` and `13083bae`, state `0` on both, each initial form view already shows `Related Search` and `Related Search Results` at the bottom | inspect the initial load state for each form first; if the panel is already visible, count it directly instead of searching for hidden tabs or expansion controls |
| The mandatory field with a `Suggestion` button beside it on ServiceNow `Create Incident` vs `Problem` forms | trajectories `1d1cac57` and `13083bae`, state `0` on Incident shows `Short description` with a `Suggestion` button, and state `0` on Problem shows `Problem statement` with a `Suggestion` button | inspect both state `0` forms directly; read the label beside each `Suggestion` button separately and do not assume the analogous field uses the same name |
| The action that appears between `Reorder` and `Ship` on Magento admin order detail pages | trajectory `11c4dfb8`, state `5`, the order detail toolbar shows `Invoice`, `Ship`, and `Reorder` together, so the middle action is `Invoice` | inspect the order detail toolbar on the opened order first; if the page is the order view rather than the orders grid, read the visible top actions directly instead of inferring from list pages |
| Whether an order-detail toolbar has a single action between `Reorder` and `Send Email` | trajectories `2305e825` and `11c4dfb8`, state `5`, the order-detail toolbar shows `Send Email`, `Hold`, `Invoice`, `Ship`, and `Reorder` as separate buttons, so the premise of one unique action between those buttons is too narrow | inspect the order-detail toolbar first; if both endpoints are visible among several separate actions, treat the question as unsupported instead of inventing an intermediate label |
| The names of the buttons visible below an item's thumbnail after clicking `Reorder` on a Magento order detail page | trajectory `aacfe598`, state `5` is the previous order detail page with the visible `Reorder` link, and state `6` is the post-reorder `Shopping Cart` page; in state `6`, the controls below the item's thumbnail/item entry are `Move to Wishlist`, `Edit`, and `Remove item` | inspect the `Reorder` target page first; if the order-detail click lands on `Shopping Cart`, read the item-row actions under the thumbnail directly |
| The name of a Magento admin dashboard tab by relative position in the visible row | trajectory `366f17da`, state `0`, the dashboard tab strip shows `Bestsellers`, `Most Viewed Products`, `New Customers`, and `Customers`, so the second from the right is `New Customers` | inspect the initial dashboard state first; if the tab row is fully visible, count from the right edge instead of searching other admin pages |
| The link shown in the blue notice bar on a single comment thread page in the reddit-based custom forum | trajectory `2e8f6477`, state `9`, the page shows `Viewing a single comment thread.` and the clickable link `View all comments` immediately below it | inspect the single-thread state first; if the notice bar is present, reuse the adjacent `View all comments` link text directly instead of broad searching the thread tree |
| The confirmation pop-up banner after bulk `Update Attributes` on the Magento product listings page | trajectory `e9a95d4f`, state `22`, right after saving on the Products grid, the green-check banner says `Task "Update attributes for 16 selected products": 1 item(s) have been scheduled for update.` | inspect the post-save Products grid state first; if the green-check success banner is visible, reuse its exact text instead of inferring a generic save message |
| The only mandatory field in the Magento bulk `Update Attributes` dialog's `Advanced Inventory` tab | trajectory `90d36b63`, state `22`, the selected `Advanced Inventory` tab shows a single asterisk-marked field, `Qty *` | inspect the open tab state directly; if `Qty *` is the only starred label, reuse `Qty` as the mandatory field without searching other tabs or the underlying product form |
| Another user's home/profile page asks for the fourth gray tab | trajectory `8012d4f3`, state `4`, the profile page at `http://localhost:9080/user/ziostraccette` shows only `Overview`, `Submissions`, and `Comments` in the gray tab row | inspect the profile-page tab row directly first; if only three gray tabs are visible, treat the premise as absent/uncertain instead of guessing a fourth label |
| Whether Magento address-book links change billing vs shipping defaults | trajectory `6d5f3f24`, states `3-4`, the Address Book page shows separate `Default Billing Address` and `Default Shipping Address` sections with distinct `Change Billing Address` and `Change Shipping Address` links; clicking `Change Shipping Address` opens the `Edit Address` page for that shipping address | inspect the Address Book page first; if both default-address sections are visible, reuse the per-link mapping directly and answer that each link edits only its own default address |
| The customer service phone number on a Magento-based custom shopping site | trajectory `6d5f3f24`, state `1`, the My Account page shows the most recent order in `Recent Orders`, and the opened order detail page shows `T:` with the same clickable phone number `6505551212` under both `Shipping Address` and `Billing Address` | inspect `My Account` first, then open the most recent order detail page and read the visible `T:` phone link directly; do not stop at the Contact Us form when the question asks for the customer-service number |
| The setting named in the Product Views Report banner that says to refresh lifetime statistics | trajectory `2a99f536`, state `2`, the banner reads `For accurate reporting, be sure to refresh lifetime statistics whenever you change the time zone.` | inspect the Product Views Report banner directly and reuse the exact setting phrase from the banner; this is a direct text lookup, not a broader Magento settings search |
| Marking a ServiceNow Problem as duplicate and asking which fields are in the popup | trajectory `110d48d3`, states `47-48`, the `Mark Duplicate` dialog on the Problem record shows `Duplicate of` plus disabled `Resolution code = Duplicate` and `State = Closed` controls, and none of the note/comment fields from options C-H are present | inspect the popup itself on the Problem record; reuse the exact visible fields and treat disabled controls as part of the dialog while ignoring unrelated note/comment fields |
| The number of preapproved change model cards with `Success rate: 100%` after `Change Requests > New` | trajectory `afa62eac`, states `3-5`, the flow goes from `Change Requests` to `New` to the change model landing page, and state `5` shows four preapproved cards explicitly labeled `Success rate: 100%` (`Add network switch to datacenter cabinet`, `Reboot Windows Server`, `Decommission local office Domain Controller`, and `Clear BGP sessions on a Cisco router`) | inspect the state `5` model-card grid first; if the same four cards are visible with `Success rate: 100%`, answer `four` without opening any other change page |
| How to return to the original `Managing Your Existing Expenses` task after editing the matching expense records and mark it complete | trajectory `1cf3ac18`, states `43-45`, the task page uses `History` to reopen `Private Task, Managing Your Existing Expenses` and then sets `State` to `Closed Complete` | inspect the task's `History` menu first; if the recent private-task link is visible, reuse it directly and finish by changing `State` to `Closed Complete` |

## Strategies

| When | Try first | Gotcha |
|---|---|---|
| A reddit-style custom forum query asks for the forum with the second-most submissions | inspect the initial homepage/subscribed feed state and count repeated forum names in the visible submissions | do not jump to the top forum only; the answer can be the next-most frequent forum name in the same visible feed |
| A Magento bulk-attribute update query asks what field in the `View Details` dialog indicates success | inspect the `View Details` dialog after `Save` and read the field label/value pair directly | do not assume the field is called `Result`; in this span the visible success field is `Summary`, and the value carries the success state |
| A Magento checkout question asks what appears in `Order Summary` after pressing `Next` from Shipping | inspect the state transition span that crosses the `Next` click and read the `Order Summary` table on the review page | do not guess from the shipping step alone; the answer is often only visible after the page advances to Review & Payments |
| A Magento order-confirmation question asks for a link positioned relative to `Print receipt` | inspect the success-page span and verify intervening links before answering | do not assume the next visible link is directly adjacent; the order-number link can sit between `Print receipt` and `Continue Shopping` |
| A Magento storefront question asks for the minimum actions needed to cover the latest twelve orders | inspect `My Account` first, then use `View All` into `My Orders` and check whether the list is paginated at 10 per page | do not stop at the first orders page; twelve orders require advancing past page 1 when only 10 rows are shown |
| A Reddit-style forum question asks for the sum of upvotes and downvotes on a new post | inspect the post page immediately after submission and read the vote summary line, especially the `point` total | do not count UI affordances separately if the page already exposes a `1 point (+1, −0)`-style summary; use the displayed total |
| A query asks for dropdown/combobox options on a ServiceNow record form | open the selected trajectory span and read the expanded control directly, including option order and any `-- None --` sentinel | do not infer hidden extras from the field label or from a neighboring field; if the dropdown is already open, count only the visible suggestions |
| A ServiceNow `Hardware Asset` `Personalize Form` query asks which checkbox labels contain a substring | inspect the expanded `Form Fields` menu and match the literal substring against each checkbox label | do not overmatch on nearby depreciation-related labels; if the exact substring is absent, treat the premise as unsupported |
| A query names a specific UI control but the haystack may implement a different control family | check `summary.json` and the selected trajectory span before broad search; look for the actual control labels or URL parameters | do not transfer a plausible interaction from the question text to the page if the span shows bucket links, dropdowns, or URL-based filtering instead |
| A ServiceNow question asks how many of several direct create forms have subtabs | inspect each state `0` form for an actual tab strip such as `Section Tab Lists` before counting | do not count `Related Links` as a subtab surface, and do not assume `User` has the same tab strip just because adjacent create forms do |
| A ServiceNow Service Catalog search-results question asks how many items show per page by default | inspect the rows-per-page selector on the search results page directly | do not count rendered rows or borrow pagination defaults from other lists; use the selected value shown in the selector itself |
| A Magento product-list query mentions price filters | inspect the category-page span for `Shopping Options -> Price` and any `price=...` URL state before hunting for input fields | editable min/max boxes are a near-match trap here; the correct evidence may be a bucket list or a direct URL filter, so the safest output can be abstain |
| A top warning banner names a destination page such as `Bulk Actions Log` | follow the banner link first, then inspect the destination page's table header row | do not treat the banner label as the answer; the column names may only appear after navigation |
| A Magento search-result query asks to filter out items below a threshold like `$100` | inspect the exact search-result span for a price bucket or other filter on that page before reusing any nearby listing/category evidence | do not promote a `$100.00 - $199.99` bucket from a different Magento listing page into a direct answer for the search-result page; this run only proved a near match |
| A Magento home-page search asks for sorting by highest rating first | inspect the search results span and read the actual `Sort By` dropdown options before assuming a rating control exists | do not infer a hidden rating sort from product `Rating:` text in the listings; if the dropdown only shows `Product Name`, `Price`, and `Relevance`, the requested sort is not supported in that flow |
| A Magento price-bucket query asks which links appear after selecting a range | inspect the post-filter `Now Shopping by` block in the same state as the selected price range | the answer is often the two maintenance links next to the selected range; do not keep searching the category page for unrelated navigation links |
| A ServiceNow deletion question asks which related records are automatically affected | inspect the user-delete confirmation dialog span before broad search | the dialog often enumerates the full set directly; if the named record type is not in the list, do not infer it from the generic "related records" wording |
| A ServiceNow offboarding query asks which applications to visit before deleting a user | inspect the offboarding span first; it should run through `Assets -> Portfolios > Hardware Assets` and then `System Security -> Users and Groups -> Users` | do not swap in `User Administration` from the navigator results; it is a nearby distractor, not the chosen route |
| A ServiceNow Problem `Mark Duplicate` popup asks which fields are actually present | inspect the dialog on the Problem record itself and count disabled controls as present fields | do not borrow note/comment fields from other duplicate-resolution workflows; if the popup only shows `Duplicate of`, `Resolution code`, and `State`, then absent note fields are evidence against those options |
| A ServiceNow asset-management list question asks for an item's class | inspect the `Assets` list row and read the `Class` column on the matching asset before opening the record | do not chase the record details or assume the class is hidden elsewhere; on this page the class can already be visible on the list row |
| A WebArena shopping query asks for an order containing an item within a date range | check whether `My Orders` only provides date, status, and `View Order`; if so, the item search happens only after opening candidate orders | do not assume there is a direct timeframe/item filter on the orders list; the correct path may be date scan plus per-order item inspection |
| A ServiceNow incident form shows an `Assigned to` autocomplete popup before any typing | inspect the field state and popup title together; the popup title is usually the field label, while the suggestion list may carry a separate `AC ...` listbox name | do not overfit the listbox role name to the visible title; the user-facing box title can stay `Assigned to` even when the internal listbox has a different label |
| A ServiceNow laptop-order query asks whether SSD size is configurable | search the `Service Catalog` / `Requested Items` flow for the exact laptop product name first, then inspect the order form for `What size solid state drive do you want?` | do not map the prompt to a different laptop label or to a hardware asset page; the evidence lives in the order form configuration, not the generic asset record |
| A ServiceNow laptop-order query asks for the price delta shown inline on an SSD option | inspect the exact catalog item page and read the SSD choice label after toggling `250 GB` / `500 GB` | do not chase a separate OS field or a different record; the delta can be embedded directly in the SSD radio label, e.g. `250 GB [subtract $300.00]` or `500 GB [add $300.00]` |
| A ServiceNow loaner-laptop query asks whether duration is preset and date is typed | inspect the `Loaner Laptop` catalog item form and read the control types directly: duration is a preset combobox, date is a free-text textbox | do not flatten both prompts into the same widget type; the exact answer depends on the field widget, not just the presence of a date-related label |
| A ServiceNow hardware catalog comparison asks which item page needs the most pre-`Order Now` changes | inspect each named item page before `Order Now` and count distinct visible widgets on the item surface, including optional software groups and free-text requirement fields | do not use the catalog search result or the later request/checkout page as the comparison surface; the extra steps live on the item form itself |
| A ServiceNow expense duplicate-removal query asks which fields matter most in the workflow | inspect the `Managing Your Existing Expenses` article before the incident/form page; the decision rule itself names the fields that matter | do not overfit to the page title or to a generic expense form; the reusable signal is the duplicate-resolution rule set, not the UI control labels |
| A ServiceNow incident-assignment query mentions matching rules, category-to-priority lookup, or assigning incidents to relevant agents | inspect the `Knowledge & Catalog - Knowledge` results list first and search for `Work Assignment: Assign Incidents to Relevant Agents` or `KB0010138` | do not jump to the incident list or a generic assignment form; the reusable clue can already be in the search-result snippet |
| A ServiceNow `Expense Lines` query asks how to mark hashtag-matching duplicate rows for merge review before deletion | trajectories `435237ce`, states `35-43`, and `1cf3ac18`, states `17-24`, the list view exposes per-row `Select record for action` checkboxes and the bulk `Actions on selected rows` menu; `Delete with preview...` is the review-before-delete path | inspect the filtered list first; select the duplicate rows with the checkbox controls, then use `Actions on selected rows` -> `Delete with preview...` instead of searching for a separate merge-review control |
| A ServiceNow `Expense Lines` duplicate-cleanup query asks for the next field after the common duplicate-check fields | inspect the field selector/open filter list on the expense line page and map `Task` to the selector’s `Parent` label before deciding | do not skip the alias check; the field may be shown under a different name than the question uses, and `Source ID` can be the next actionable field |
| A reddit-style custom forum biography-edit query asks for the button name after saving | inspect the post-save right-hand profile block first; the actionable label there is the answer, while the preview below the editor is a distractor | do not infer from the biography text area or preview block, since the visible control is often only in the adjacent profile card after save |
| A ServiceNow form-comparison query asks whether `Create Incident` or `Problem` has `Related Search` at the bottom | inspect state `0` for both trajectories and check the visible footer panels directly | do not assume the feature is hidden or requires interaction; both forms can expose `Related Search` immediately on load |
| A ServiceNow create-form comparison asks whether several forms all prepopulate a system-generated case number | inspect each candidate at state `0` and compare the visible `Number` field directly | do not assume every create form has `Number`; Change Request, Problem, and Incident can show it while Hardware and User may not |
| A ServiceNow create-form comparison asks which form has more mandatory fields | inspect the direct create forms and count only controls whose labels literally include `Mandatory - must be populated before Submit` | do not count fields that are merely filled or later required in a workflow; compare the visible form-state marker, not the task's requested values |
| A query asks for a field that is both required and read-only on one of two ServiceNow create forms | inspect both state `0` forms and check the same field for the `Read only - cannot be modified` badge and the required marker on the live form | do not transfer a read-only required field from Hardware to Change Request; the answer depends on the exact initial form state |
| A ServiceNow comparison asks for a shared mandatory read-only field between `Create Change Request` and `Hardware` | inspect both state `0` forms and require the same label to appear with both the mandatory marker and the `Read only - cannot be modified` badge | do not carry Hardware's `Quantity` over just because it is mandatory-read-only there; if Change Request only shows read-only fields like `Type`, `Conflict status`, and `Conflict last run`, abstain instead of forcing a shared name |
| A ServiceNow query asks for a shared mandatory field between two create forms | inspect both state `0` forms and compare the literal `Mandatory - must be populated before Submit` markers on the same label | do not promote a plain visible overlap like `Model` on `Create Change Request` into a required shared field when only the other form marks it mandatory |
| A ServiceNow comparison asks for the analogous mandatory field with a `Suggestion` button on Incident vs Problem | inspect both create forms at state `0` and match each `Suggestion` control to its own visible label | do not canonicalize similar fields across forms; Incident can say `Short description` while Problem can say `Problem statement` |
| A ServiceNow change-model query asks how many preapproved cards have `Success rate: 100%` | inspect `Change Requests` -> `New` and count only the model-card tiles on the landing page that explicitly show `Success rate: 100%` | do not count cards from a different change page or infer the total from the task text; the count comes from the visible card labels on the model-card grid |
| A Magento admin order-detail query asks for the action between two named toolbar buttons | inspect the opened order detail page and read the top toolbar action order directly | do not transfer the action order from the Sales > Orders grid or from another status; the toolbar is visible on the order view itself and may be enough to answer without any extra navigation |
| A Magento admin order-detail query asks for the action between `Reorder` and `Send Email` | inspect the order-detail toolbar and verify whether the buttons are actually adjacent | do not assume there is exactly one action between them; this toolbar can expose several separate buttons, making the premise unsupported rather than a fill-in-the-blank lookup |
| A Magento admin dashboard query asks for a tab by relative position in the visible tab row | inspect the dashboard's initial state and count tabs from the right edge | do not navigate away when the row is already visible; the answer is usually a direct position read, not a hidden menu or secondary page |
| A Magento admin `ORDER VIEW` query asks for the tab between `Transactions` and `Shipments` | trajectory `8db2b8cf` state `3` and trajectory `2ab85f90` state `7`, the visible tab row was `Information`, `Invoices`, `Credit Memos`, `Shipments`, and `Comments History`, with no `Transactions` tab present | inspect the `ORDER VIEW` tab row directly; if `Transactions` is absent, treat the premise as wrong and abstain instead of inventing an in-between tab |
| A custom forum query compares the user's own profile tabs with another user's profile tabs | inspect both gray profile tab rows directly and compare the visible labels | do not confuse the profile tab row with the account menu or settings page; if both pages show `Overview`, `Submissions`, and `Comments`, the difference is zero |
| A reddit-based forum query asks for the link associated with the blue single-thread notice above `View all comments` | inspect the single comment thread state and read the link text adjacent to the notice | do not confuse the notice text `Viewing a single comment thread.` with the answer; the reusable target is the link label itself |
| A reddit-style subreddit posting query asks for poll creation controls | inspect the subreddit page and the follow-on `Create submission` page directly; in this flow the visible actions are `Submit` on the subreddit page and `URL` / `Image` on the create page | do not promote a nearby posting flow into a poll flow when no `Poll` button or link is visible; treat that as unsupported rather than guessing the next click |
| A Magento admin bulk-attribute update query asks for the confirmation banner after clicking `Save` | inspect the post-save Products grid state and read the green-check success banner verbatim | do not generalize from the action name alone; the exact banner text can include the selected-product count and the scheduled-update wording |
| A Magento bulk `Update Attributes` query asks for the mandatory field in `Advanced Inventory` | inspect the selected `Advanced Inventory` tab directly and look for the only asterisk-marked label | do not assume the required field is anywhere else in the dialog; in this span the sole mandatory field is `Qty *`, so the reusable answer is `Qty` |
| A Magento product-grid bulk-action query asks how to reach `Archived` | inspect trajectory `e9a95d4f` states `17-20` first: selected products on the grid, the bulk `Actions` menu with `Change status`, then the `Update Attributes` page with `Save` | do not infer an `Archived` option from `Change status` alone; this span supports only the bulk-update path and does not directly confirm an archive state |
| A reddit-style custom forum query asks for a tab by ordinal position on another user's profile page | inspect the gray tab row on the profile page itself and count only the visible labels | do not infer a missing fourth tab from similar sites or from the question wording; if the span only shows `Overview`, `Submissions`, and `Comments`, the correct guard is that no fourth tab is evidenced |
| Comparing own profile page with another user's profile page to count extra top tabs | trajectories `2d00b63d` state `2` and `8012d4f3` state `4`, both profile pages show the same gray tab row: `Overview`, `Submissions`, `Comments`, so there are no extra tabs on the user's own page | inspect the gray profile tab row on both pages first; if the visible labels match, answer zero rather than searching for hidden account/settings controls |
| A Magento order-detail query asks what opens when clicking a line-item product name | inspect the `Items Ordered` section first and verify whether the product name is actually a link | do not transfer the nearby product-page `Details` tab to the order line item; in the supporting span the line-item name is plain text, so there is no product page/tab opened from that click |
| A Magento `Reorder` click lands on the `Shopping Cart` page and the question asks for the item-row buttons | inspect the post-reorder cart row under the item's thumbnail first | do not keep searching the original order detail page for the controls; the answer lives in the cart row actions (`Move to Wishlist`, `Edit`, `Remove item`) |
| A reddit-style custom forum question asks for the option below `All time` in the Top-sort timeframe dropdown | inspect the expanded Top-sort timeframe dropdown directly and read the last visible option | do not invent a further option when `All time` is already the final item; if the dropdown ends there, treat the premise as absent and abstain |
| A mixed Magento navigation question asks whether two actions both change pages and both alerts contain links | inspect each post-action URL and the visible alert anchor text separately | do not transfer behavior from `Add to Cart` to `Add to Wish List`; one action can stay on-page while the other navigates |
| A Magento Orders-page fraud-status query asks for a `resolved fraud suspect` label or similar | inspect the Orders grid `Status` column and the order detail page's `Status` control directly; the only fraud-related option evidenced here is `Suspected Fraud` | do not promote a generic fraud-suspect phrase into a distinct status label; if the exact text is absent, treat the premise as unsupported rather than searching for a separate resolved-fraud state |
| A Magento Contact Us form shows `Phone Number` as an input field | inspect the page context and form controls first; the label `Phone Number` can be just a required contact field, not a support number | do not treat the contact form's own `Phone Number` textbox as evidence of a customer-service phone number on the page |
| A Magento customer-service phone query points at the My Account / recent-order view instead of Contact Us | inspect the opened order detail page under `Recent Orders` and read the `T:` phone link in `Shipping Address` or `Billing Address` | do not confuse a visible contact-field label with the support number; the order detail page can carry the reusable phone evidence directly |
| A Magento product detail page review block query asks which review fields are visible | inspect the opened `Reviews` tab and enumerate the per-review card labels only | do not count the tab label, the aggregate star summary, or surrounding page chrome as review-block fields; the card labels here are `Review by`, `Posted on`, review title, star rating, and review text |
| A Magento address-book query asks whether `Change Billing Address` or `Change Shipping Address` affects both defaults | inspect the Address Book page and the follow-on `Edit Address` page directly | do not infer a cross-updating default from a link label alone; the evidence here shows each link edits only its matching billing or shipping address |
| A reddit-based forum query asks for `Rising` sort or for the first post under that sort | inspect the forum sort menu on the list page first | do not assume `Rising` exists because the site is forum-like; in this trajectory the visible options are `Hot`, `New`, `Active`, `Top`, `Controversial`, and `Most commented`, so the premise is unsupported |
| A Magento admin report question asks for the column immediately to the right of a named header | inspect the visible table header row in the report page before broad search | admin reports often expose the answer as an ordered header sequence, so prefer direct left-to-right reading over hunting for a separate column settings control |
| The column immediately to the right of `Quantity` in the Magento Admin `Low Stock Report` table | trajectory `01b7cff8`, state `2`, the header row is visible as `Product`, `SKU`, `Quantity`, `Source Code` | inspect the report table header row directly; if the visible sequence matches, reuse `Source Code` without searching for another report or column chooser |
| A ServiceNow Catalog Items list-view question asks for a header between `Class` and `Delivery time` | inspect the `Catalog Items` header row on `sc_cat_item_list.do` first; in the observed span the visible sequence is `Price`, `Class`, `Delivery time` | do not invent an intervening column when the header row is already a three-item sequence; treat the premise as wrong and abstain if the same ordering appears |
| A ServiceNow Self-Service ordering query asks which modules to use to order the item someone requested | trajectory `3c588c61`, states `0-5` and `16-19`, the Self-Service nav first opens `Requested Items` and then switches to `Service Catalog` to find and order the item | inspect the Self-Service menu path first; reuse `Requested Items` then `Service Catalog` in that order instead of starting from the catalog and trying to backfill the request list |
| The Self Service module for extra device item requests for agents selected from incident-report criteria | trajectory `096432bf`, state `3`, the page title is `Requested Items View: Self Service`; trajectory `0b072635`, states `6-7`, the workflow reaches the `Requested Items` list through `Open Records > Items` | inspect the `Requested Items` page/title first; if it is visible, reuse `Requested Items` and do not default to `Service Catalog` |
| Whether Magento `Add to Cart` and `Add to Wish List` each jump to a new page and whether both success messages contain a link | trajectories `1e58cea9` state `11-12` and `1b41458b` state `5-6`, `Add to Cart` stays on the current product page and shows an alert with a clickable `shopping cart` link, while `Add to Wish List` jumps to the wishlist page and shows an alert with a clickable `here` link | inspect the post-click URL and alert text for each action separately; do not assume both actions behave the same or that a success message without a URL change still means a new page |
| A Magento Product Views Report question mentions refreshing lifetime statistics after changing a setting | inspect the banner text on the Product Views Report page and preserve the exact setting name it names | do not swap in a nearby Magento setting such as `currency` when the banner explicitly says `time zone`; treat that as a premise mismatch |
| A Magento Product Views Report banner asks what change requires refreshing lifetime statistics | inspect the banner text verbatim on the Product Views Report page | do not generalize to unrelated report settings; the reusable answer comes from the exact phrase `time zone` in this specific banner |
| After following the Magento top warning banner to `Bulk Actions Log`, what table columns are shown there | trajectory `06c70de4`, states `12-17`, state `12` shows the System Messages warning panel with a visible `Bulk Actions Log` link, and state `17` on `/admin/bulk/index/` shows the log table header row with `ID`, `Start Time`, `Description of Operation`, `Status`, and `Action` | inspect the warning banner link first; if it lands on the log page, read the header row directly instead of searching for a separate column chooser |
| When a reddit-style question asks how to reply to an existing comment instead of the whole post | inspect the comment thread for the inline `Reply` control first, then inspect the nested composer for the publish button | do not transfer post-level composer controls into the comment-reply flow; the nested reply action here is `Reply` then `Post`, not a generic top-level submit path |
| A ServiceNow offboarding query asks why hardware still appears assigned after clearing `Assigned to` | trajectory `f92c1020`, states `20-22`, the hardware list was still filtered by `Assigned to` because the reference field had been cleared superficially and the change had not fully committed | inspect the field edit state first; fully blank the `Assigned to` reference, tab out so `onchange` fires, then `Update` again before trusting the list result |
| A ServiceNow offboarding question asks which applications to visit before deleting a user | trajectory `f92c1020`, states `34-35`, the offboarding span first works in `Assets` via `Portfolios > Hardware Assets` to clear the hardware `Assigned to` field, then navigates to `System Security -> Users and Groups -> Users` to delete the user record; `User Administration` appears as a nearby navigator result but was not the chosen path | inspect states `34-35` first; reuse `Assets, System Security` when the prompt asks for the application pair, and treat `User Administration` as a nearby distractor |
| A ServiceNow `Expense Lines` task asks what to do after filtering on a hashtag and choosing duplicate rows to remove while keeping the most expensive duplicate | trajectory `3fafa5c3`, states `48-59`, the filtered list shows matching rows selected and the next step is the list-level `Actions on selected rows` menu; `Delete with preview...` is the relevant bulk-delete path | inspect the filtered list first; select the rows to remove, then use `Actions on selected rows` -> `Delete with preview...` instead of opening each expense line |
| The combined rescheduling rule for ServiceNow change requests filtered by `#SERIES-ac17456f-8` | trajectory `fc0e8ec3`, `Goal` section at `TRAJECTORY_SUMMARY_FULL.md:1463-1472`, the task text says higher impact changes go first, consecutive changes may be at most 1 hour apart, and durations are fixed by risk as High 3 days, Moderate 2 days, Low 1 day | inspect the goal/task text first; if the query matches this series filter, reuse the exact rule wording before searching the ServiceNow pages |
| A query asks why quick column filters on a ServiceNow asset list show no rows even though matches exist | trajectory `e9f683bd`, states `4-5`, the asset list with `Computer` and `In maintenance` entered into the quick column filters returns `No records to display`; trajectory `dc083cd0`, states `7-8`, shows the intended path starting from `Show / hide filter` and then the filter builder | inspect the filter-builder span first; if the quick column row is empty or misleading, reuse the `Show / hide filter` route instead of treating the column search boxes as the right workflow |
| A ServiceNow `Related Links` lookup on the `Data Management Delete Job` form | trajectory `3fafa5c3`, state `54`, on `Data Management Delete Job DM0001009` the visible `Related Links` region contains `Preview Cascade` and `Execute Now` | inspect the exact form first; if the question names the form and asks for links under `Related Links`, reuse the section contents directly instead of scanning nearby menus or footer links |
| A ServiceNow `Condition` section question on the `Data Management Delete Job` form asking for the two buttons besides `AND` and `OR` | trajectory `f92c1020`, state `43`, the visible `Condition` section on `Data Management Delete Job` shows `Preview`, `Add OR Condition`, `Add AND condition`, and `New Criteria`, so the two buttons besides `AND` and `OR` are `Preview` and `New Criteria` | inspect the `Condition` section first; if it is visible, read all button labels in place instead of broad-searching the form |
| The two new fields on the Data Management Delete Job progress page after successful execution | trajectory `3fafa5c3`, states `69-70`, the `Progress` table goes from `State = Starting` with `Message = Waiting for the background job to start` to `State = Complete` with `Completion code = Success` and `Message = Finish processing request: DM0001009` | inspect the `Progress` table after `Proceed`; if the completed status page is already open, read `Completion code` and `Message` directly instead of re-opening the job form |
| A ServiceNow shared-mandatory-field query comparing `Create Change Request` vs `Hardware` | trajectory `054c4ed2` state `0` shows Hardware marks `Model category`, `Model`, `Serial number`, and `Quantity` mandatory; trajectory `6cd3cdc4` state `0` shows Change Request has a visible `Model` field but no `Mandatory` or `required` marker | inspect both state `0` forms first; if the overlapping label is not mandatory on both sides, treat the shared-mandatory premise as false and abstain |
| Which ServiceNow form has more mandatory fields between `Create Change Request` and `Hardware` | trajectories `42b931a1` and `6cd3cdc4`, the hardware `alm_hardware.do` create form states `10-17` show the exact `Mandatory - must be populated before Submit` marker on `Model category`, `Model`, `Serial number`, and `Vendor`, while the change request `change_request.do` state `0` did not show that marker in the inspected span | inspect the Hardware create form first and count only labels with the literal mandatory marker; if the Change Request span lacks it, reuse Hardware as the answer |
| The count of `Model category` options on the ServiceNow Hardware form | trajectory `42b931a1`, state `11`, the Hardware new-record form's `Model category` autocomplete shows exactly two visible suggestions, `Computer` and `Computer Peripheral`, and the UI reports `2 suggestions` | inspect the expanded autocomplete first; if the suggestions count is visible, answer `two` directly instead of searching later states |
| A ServiceNow reference field looks cleared but the list/detail view still shows the old value | tab out or click away to force `onchange`, then press `Update` and re-check the result | a superficially blanked reference can fail to commit until blur fires, so the old assignee or relation may still appear |
| A ServiceNow change-request rescheduling query mentions impact order, a tight gap, and risk-based durations | search the goal/task text for the exact trio `higher impact first`, `1 hour`, and `High/Moderate/Low` duration mapping | do not swap impact for risk or relax `1 hour` to `1 day`; the rule set is exact and the constraints travel together |
| Quick column filters on a ServiceNow list return no rows even though the task expects matches | jump to `Show / hide filter` and use the list filter builder, then re-check the result set | do not assume the inline column search row is the intended search path; the quick filters can be a dead end here |
| A ServiceNow asset-list `Assigned to` search box is filled but the hardware rows still include other employees | trajectory `4c9eaae0`, states `6-7`, on the Hardware Assets list state `6` shows `Search column: assigned to` filled with `Julia-Dylan Ray-Mclean` and state `7` only narrows after `Enter` | inspect the active column search box first; if the value is visible but the list has not changed yet, press `Enter` before assuming the filter is wrong |
| A ServiceNow list header search box is filled but the rows have not changed yet | press `Enter` in the active `Search column: ...` box and re-check the list or URL | a typed quick filter may still be pending submission; do not treat the pre-submit state as proof that the filter is wrong |
| A ServiceNow query asks for entries or buttons under a named in-page section such as `Related Links` or `Condition` | inspect that exact section on the form before broad search and read only the items inside it | do not merge nearby page controls, sidebars, or footer links into the named section; compound controls like `Add AND condition` should be read as full button labels, not stripped down to `AND` or `OR` |
| A ServiceNow Data Management Delete Job status question asks about the progress page after the job completes | inspect the `Progress` block on the worker/status page, not the list or form view; the completed screen exposes `State`, `Completion code`, and `Message` | do not infer the answer from `Related Links` or the form header; the completion fields appear only after the background job runs |
| A ServiceNow create-record success banner names a specific auto-created asset or device after submit | inspect the top-page info popup immediately after saving and read the literal object created in the message | do not collapse a specific banner like `Primary email device created` into a generic `Record inserted` or `Update successful`; the popup text is the confirmation target |
| A ServiceNow Self-Service query asks which module to use for item requests, or asks for the first step in the ordering path | inspect the Self-Service trail first; `Requested Items` is the module answer when the page title says `Requested Items View: Self Service`, and `Open Records > Items` is another direct route into the same list | do not default to `Service Catalog`; it can be the later ordering step, not the module name |
| A ServiceNow onboarding multiple-choice question asks for the correct major-step sequence | trajectory `2ebe08f4`, state `0`, the task description itself enumerates the onboarding flow as `Create a User -> order Apple Mac Book Pro15 -> create a Hardware Asset`, which matches the correct step order directly | inspect the initial task description first; if it already names the sequence, reuse that exact order and do not infer a later portal flow or normalize the object labels into a different record type |
| A ServiceNow form-comparison query asks for a field shared by `Hardware` and `Create Change Request` | inspect both direct create forms at state `0` and compare the visible main-form labels and required markers | do not use read-only-only fields or later workflow steps as the shared field; the answer here came from the base forms and the required `Model` label on Hardware |
| A ServiceNow form-comparison query asks for the only field that is both mandatory and non-mutable on one of two create forms | inspect the Hardware `New record` and Change Request `create` forms at state `0`, then check the same field for the `Read only - cannot be modified` badge and the required marker | do not transfer a read-only required field from Hardware to Change Request; the answer depends on the exact initial state and is `Quantity` only when Hardware shows both badges together |
| A ServiceNow query asks for a shared subform tab on `Create Change Request` vs `Hardware` | inspect the state `0` Change Request form and the state `10` Hardware form tab strips directly | do not infer a shared tab from mandatory-field overlap; here the visible strips are `Planning, Schedule, Conflicts, Notes, Closure Information` vs `General, Financial, Disposal, Depreciation, Contracts, Entitlements, Activities`, so the premise is false |
| A ServiceNow form-comparison query asks whether any candidate preloads `Category = Hardware` on initial load | inspect state `0` of each candidate and compare the literal field label/value in that same state | do not reuse a later Problem-form selection, do not treat `Model category` on Hardware as `Category`, and do not assume the User form has a Category field |
| Whether ServiceNow create forms like Change Request, Problem, Incident, Hardware, and User all show a case number | trajectories `054c4ed2`, `132da65a`, `6cd3cdc4`, `13083bae`, and `1d1cac57`, state `0` create forms show a prepopulated `Number` field on Change Request, Problem, and Incident only; Hardware and User do not show one | inspect each form at state `0` and count only the visible prefilled `Number` labels; if Hardware or User lacks it, the answer is false |
| Counting how many of `Create Change Request`, `Problem`, `Incident`, `Hardware`, and `User` have subtabs on the page | trajectories `6cd3cdc4`, `13083bae`, `1d1cac57`, `054c4ed2`, and `132da65a`, state `0`; Change Request, Problem, Incident, and Hardware each show a `Section Tab Lists` strip, while User shows only `Related Links`, so four forms have subtabs | inspect the state `0` forms one by one and count only a visible tab strip; do not treat `Related Links` as a subtab surface |
| A ServiceNow Service Catalog item-page query asks for the names of the two right-side sections on a specific hardware item page | inspect same-surface Hardware catalog item pages first and read only the visible right-hand section headers | do not promote a nearby hardware catalog item template into proof for the exact item page; if the target page was not directly captured, treat the template as a reuse hint, not a direct answer |
| A ServiceNow Service Catalog query asks for exact item names when similar laptop variants exist | inspect the catalog search results and read the item labels character-for-character before opening the item | do not normalize `Developer` vs `Development`, do not convert OS choices on the item page into catalog item names, and do not assume Mac, PC, Linux, or Chromebook variants share one label; if the exact titles are not visible in the search results, treat the premise as unsupported |
| A ServiceNow catalog task asks whether a specific optional software checkbox exists on a laptop item | verify the catalog item title against the task text, then inspect the `Optional Software` section directly | do not transfer software options between nearby laptop items; if the exact checkbox is absent on the named page, treat the premise as false |
| A ServiceNow `Expense Lines` duplicate-cleanup task already identified the rows to remove | stay on the filtered list and use the row checkboxes plus `Actions on selected rows` -> `Delete with preview...` | do not open each record page or look for a separate merge-review control; the bulk preview is the review step |
| A ServiceNow `Expense Line` record-page query asks for the field with a checkbox next to it | inspect the record form state first and read the checkbox label under the record header | do not confuse the base record form with duplicate-cleanup selectors or list-view filters; the checkbox label can be the direct answer |
| A ServiceNow `Private Task` form-page query asks which fields have checkboxes next to them | inspect the form section at state `0` and read the checkbox labels literally | do not borrow checkbox labels from another ServiceNow form or record page; on this page the visible checkbox fields are `Active` and `Work notes` |
| The single-word field unique to ServiceNow `Create Change Request` vs `Incident` | trajectories `6cd3cdc4` and `1d1cac57`, state `0` on both, `Create Change Request` shows a blank `Justification` textbox in the `Planning` tab while `Incident` does not show a `Justification` field among the visible inputs; the trace does not expose an explicit required marker | inspect both state-0 forms and compare the visible tab content directly; if the question asks for the one unique single-word textbox, reuse it without wandering into later workflow states |
| Comparing the Agent Workload Balancing workflow with the stock-restocking workflow when excluding the shared report/chart retrieval stage | trajectory `12457787`, states `45-46`, the closing state is `Balance the workload for problems with hashtag #PRB052840832 | Private Task | ServiceNow`; trajectory `013696c4`, states `47-48`, the closing state is `Retrieve information from the chart with the title #CAT001314192 and perform the mentioned task. | Private Task | ServiceNow` | inspect the closing task-completion states first; if both wrapper pages end with `Private Task`, reuse `Private Task` directly and ignore the earlier `Reports` navigation |
| A workflow query shares an early `Reports` or chart-retrieval stage but asks for the final task-completion wrapper page | inspect the closing state titles before earlier navigation | do not promote the shared retrieval module to the answer when the wrapper page title is the real target; in this run the wrapper was `Private Task` |
| A ServiceNow task asks how to get back to the originating private task after working a list of matching records | inspect `History` and click the recent matching task link before reaching for global search | do not assume the search box will surface an exact-match shortcut; if the task page shows `No exact match. Press Enter for full results.`, `History` is the reliable return route |
| The default sort field shown in the ServiceNow `All Assets` list before changing the sort target | trajectory `49f98e9e`, states `6-7`, on the Assets list the sort row is visible in state `6` with the combobox defaulting to `Acquisition method`, and state `7` shows the user switching to `Asset function` | inspect the sort row before any selection change; if the combobox already shows a value, reuse that pre-selection label directly and do not infer it from the later chosen field |
