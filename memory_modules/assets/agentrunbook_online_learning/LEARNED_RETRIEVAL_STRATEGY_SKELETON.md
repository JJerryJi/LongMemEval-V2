# Learned Retrieval Strategy

This file contains online retrieval hints learned from previous memory queries in this run. Treat it as retrieval guidance, not as final answers.

## Past Queries

Use this section for reusable evidence locations from prior queries. Keep entries short and make the fast path obvious.

Entry format:
- Looking for: <what the prior query needed to retrieve>
  Evidence found: trajectory `<id>`, states <start>-<end>, <what the span showed>
  Fast path: inspect this span first; if it matches the current target, reuse it without broad search

Past Queries Records:

| Looking for | Evidence found | Fast path |
|---|---|---|

## Strategies

Use this section for reusable search tactics, shortcuts, exactness guards, and gotchas that make future memory search faster or more exact.

Entry format:
- When: <query shape or retrieval situation>
  Try first: <fast search route, summary terms, helper command pattern, or span inspection tactic>
  Gotcha: <near-match trap, unsupported transfer, or exactness check to avoid mistakes>

Strategies Records:

| When | Try first | Gotcha |
|---|---|---|
